[project]
name = "agent"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "langchain",
    "langgraph",
    "llama-index",
    "chromadb",
    "llama-index-vector-stores-chroma",
    "sentence-transformers",
    "fastapi",
    "pydantic-settings"
]