[project]
name = "agent"
version = "0.1.0"
requires-python = ">=3.12"

dependencies = [
    "langchain",
    "langgraph",
    "llama-index",
    "chromadb",
    "sentence-transformers",
    "fastapi"
]